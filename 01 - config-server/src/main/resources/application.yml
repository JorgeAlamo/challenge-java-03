spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/JorgeAlamo/repo-config-server/
          default-label: main
          timeout: 10
server:
  port: 8888